// import React from 'react'

// export const TagForm = ({tags, onClickHandler}) => {
//     //tags.forEach(tag => console.log(tag.name))
//     return (
//         <div className='input-container'>
//             {tags.map(tag => (
//                 // <button className='input-item' onClick={onClickHandler} value={tag.name} name={tag.name}>{tag.name}</button>
//                 <div className='input-item' onClick={onClickHandler}>{tag.name}</div>
//             ))}
//         </div>        
//     )
// }